@extends('template.index')
@section('title', '{{crudName}}')

@section('content')
<div class="row">
    <div class="col-lg-12 margin-tb">
        <div class="pull-left">
            <h2>Laravel 8 CRUD </h2>
        </div>
        <div class="pull-right">
            <a class="btn btn-success" href="{{ route('{{crudName}}.create') }}" title="Create a data"> <i class="fas fa-plus-circle"></i>
            </a>
        </div>
    </div>
</div>

@if ($message = Session::get('success'))
<div class="alert alert-success">
    <p>{{ $message }}</p>
</div>
@endif

<table class="table table-bordered table-responsive-lg">
    <tr>
        <th>No</th>
        {{listHeader}}
        <th>Action</th>
    </tr>
    @foreach (${{crudName}}s as $data)
    <tr>
        <td>{{ $data->iteration }}</td>
        {{listBody}}
        <td>
            <form action="{{ route('{{crudName}}.destroy', $data->{{id}}) }}" method="POST">
                <a href="{{ route('{{crudName}}.show', $data->{{id}}) }}" title="show">
                    <i class="fas fa-eye text-success  fa-lg"></i>
                </a>
                <a href="{{ route('{{crudName}}.edit', $data->{{id}}) }}">
                    <i class="fas fa-edit  fa-lg"></i>
                </a>
                @csrf
                @method('DELETE')
                <button type="submit" title="delete" style="border: none; background-color:transparent;">
                    <i class="fas fa-trash fa-lg text-danger"></i>
                </button>
            </form>
        </td>
    </tr>
    @endforeach
</table>

{!! ${{crudName}}s->links() !!}
@endsection